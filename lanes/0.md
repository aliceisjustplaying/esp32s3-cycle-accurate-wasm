# Lane 0: toolchain currency (LOCAL ONLY)

Runs on the maintainer's machine with the physical board. Not a cloud
lane.

Current state: the ESP-IDF 6.1 flag day is COMPLETE (see
[`../STATUS.md`](../STATUS.md)): fixtures rebuilt and pinned on v6.1,
receipt cohorts rerun, silicon-architectural headlines confirmed
unchanged, IDF-owned deltas recorded, evidence in
[`../timing/evidence/idf61-rebaseline-3db3985/`](../timing/evidence/idf61-rebaseline-3db3985/README.md).
One flag day, no toolchain mixing; historical v6.0.2 receipts stay
scoped to their original pins.

Remaining work:

1. Diagnose the systematic one-cycle first-line cache probe shift (a
   pooling-probe diagnosis), then commit an adoption disposition; until
   then that cost class stays blocked in every importer.
2. Recover the six identities still below the strict
   two-independent-receipt criterion (repeated USB capture truncation);
   two have zero receipts, four have one.

Working trees: `tinydraw` (calibration projects, fixtures) and this
repository (`../timing/`).

Exit: the first-line disposition is committed and all 210 identities
meet strict recovery, or the shortfall is dispositioned as accepted with
a recorded reason.
